name: "aws-eks-node-termination-handler"
# Canonical GitHub repo
github_repo: "cloudposse-terraform-components/aws-eks-node-termination-handler"
# Short description of this project
description: |-
  This component creates a Helm release for
  [aws-node-termination-handler](https://github.com/aws/aws-node-termination-handler) on a Kubernetes cluster.
  [aws-node-termination-handler](https://github.com/aws/aws-node-termination-handler) is a Kubernetes addon that (by
  default) monitors the EC2 IMDS endpoint for scheduled maintenance events, spot instance termination events, and
  rebalance recommendation events, and drains and/or cordons nodes upon such events. This ensures that workloads on
  Kubernetes are evicted gracefully when a node needs to be terminated.
usage: |-
  **Stack Level**: Regional

  Once the catalog file is created, the file can be imported as follows.

  ```yaml
  import:
    - catalog/eks/aws-node-termination-handler
    ...
  ```

  The default catalog values

  ```yaml
  components:
    terraform:
      aws-node-termination-handler:
        backend:
          s3:
            workspace_key_prefix: aws-node-termination-handler
        vars:
          enabled: true
          chart_version: 0.15.3
          rbac_enabled: true

          # You can use `chart_values` to set any other chart options. Treat `chart_values` as the root of the doc.
          #
          # # For example
          # ---
          # chart_values:
          #   enableShield: false
          chart_values: {}
  ```

  <!-- prettier-ignore-start -->
  <!-- prettier-ignore-end -->
references:
  - name: aws-node-termination-handler
    description: Helm Chart
    url: https://artifacthub.io/packages/helm/aws/aws-node-termination-handler
  - name: aws-node-termination-handler
    description: AWS Node Termination Handler
    url: https://github.com/aws/aws-node-termination-handler
tags:
  - component/eks/aws-node-termination-handler
  - layer/eks
  - provider/aws
  - provider/helm
# Categories of this project
categories:
  - component/eks/aws-node-termination-handler
  - layer/eks
  - provider/aws
  - provider/helm
# License of this project
license: "APACHE2"
# Badges to display
badges:
  - name: Latest Release
    image: https://img.shields.io/github/release/cloudposse-terraform-components/aws-eks-node-termination-handler.svg?style=for-the-badge
    url: https://github.com/cloudposse-terraform-components/aws-eks-node-termination-handler/releases/latest
  - name: Slack Community
    image: https://slack.cloudposse.com/for-the-badge.svg
    url: https://slack.cloudposse.com
related:
  - name: "Cloud Posse Terraform Modules"
    description: Our collection of reusable Terraform modules used by our reference architectures.
    url: "https://docs.cloudposse.com/modules/"
  - name: "Atmos"
    description: "Atmos is like docker-compose but for your infrastructure"
    url: "https://atmos.tools"
contributors: [] # If included generates contribs
